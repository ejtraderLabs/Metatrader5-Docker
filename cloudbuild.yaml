steps:
  - name: 'gcr.io/cloud-builders/docker'
    args: ['build', '-t', 'gcr.io/$PROJECT_ID/metatrader5:latest', '.']
images:
  - 'gcr.io/$PROJECT_ID/metatrader5:latest'
options:
  logs_bucket: 'gs://993708475556-global-cloudbuild-logs'  # Specify your logs bucket here.
